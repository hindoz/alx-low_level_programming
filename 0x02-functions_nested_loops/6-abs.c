#include "main.h"

/**
 * _abs - compute absolute value
 *
 * @i : tested number
 *
 * Return:  0 (Success)
 */

int _abs(int i)
{
	if (i >=  0)
		return (i);
	else
		return (i * -1);
	_putchar('\n');

}
