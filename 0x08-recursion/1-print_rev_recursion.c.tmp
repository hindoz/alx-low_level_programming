#include"main.h"
/**
 * _print_rev_recursion - fuction that print a string in reverse
 * @s : input string
 */

void _print_rev_recursion(char *s)
{
	int i = 0;

	while (s[i] != '\0')
		i++;
	while (i != 0)
	{
		_putchar(s[i]);
		i--;
	}
	_putchar('\n');
}
