#include<stdio.h>
#include"dog.h"

/**
 * print_dog - function that print struct dog
 * @d : pointer to newly struct
 * @dog : struct name
 */

void print_dog(struct dog *d)
{
	p = malloc(sizeof(struct dog));
	if (p == NULL)
		printf("nothing");
	if ((*p.name = NULL) || (*p.age = NULL) || (*p.owner = 0))
		return (nil);
}

